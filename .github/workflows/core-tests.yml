name: AssistaPay Core Tests

on:
  push:
    branches:
      - main
      - 'refactor/**'
      - 'feature/**'
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  core-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Baixar código
        uses: actions/checkout@v4

      - name: Preparar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Verificar sintaxe do app atual
        run: node --check app.js

      - name: Verificar sintaxe dos módulos centrais
        run: |
          node --check src/core/config.js
          node --check src/core/storage.js
          node --check src/core/permissions.js
          node --check src/core/auth.js

      - name: Executar testes de autenticação, permissões e armazenamento
        run: node tests/core-smoke.test.js
