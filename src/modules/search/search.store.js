(function(g){'use strict';const AP=g.AssistaPay=g.AssistaPay||{};function users(){const session=AP.session?.current?.();const profile=AP.storage?.get?.('profile',{})||{};const base=[{id:session?.id||'demo-user',name:session?.name||profile.name||'Usuário AssistaPay',username:session?.username||profile.username||'usuario',avatar:profile.avatar||''}];const saved=AP.storage?.get?.('users',[])||[];return [...base,...saved].filter((u,i,a)=>u&&a.findIndex(x=>x.id===u.id)===i);}AP.searchStore=Object.freeze({users});})(window);