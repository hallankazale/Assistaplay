(function(global){'use strict';const AP=global.AssistaPay=global.AssistaPay||{};function apply(root=document){root.querySelectorAll('[data-save]').forEach(button=>{const active=!!AP.profileFavorites?.has?.(button.dataset.save);button.textContent='🔖';button.classList.toggle('is-saved',active);button.setAttribute('aria-pressed',active?'true':'false');});}document.addEventListener('click',event=>{const button=event.target.closest('[data-save]');if(!button)return;setTimeout(()=>apply(button.closest('.ap-video-slide')||document),0);});const observer=new MutationObserver(()=>apply(document));observer.observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('DOMContentLoaded',()=>apply(document));AP.feedSaveVisual=Object.freeze({apply});})(window);