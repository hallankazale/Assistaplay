(function(global){
'use strict';
const AP=global.AssistaPay=global.AssistaPay||{};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const readProfile=()=>AP.storage?.get?.('profile',{})||AP.storage?.read?.('profile',{})||{};
function writeProfile(value){if(AP.storage?.set)AP.storage.set('profile',value);else AP.storage?.write?.('profile',value);}
function profile(){const saved=readProfile(),user=AP.session?.user?.()||{};return{name:saved.name||user.name||'Usuário AssistaPay',username:saved.username||user.username||'@usuario',bio:saved.bio||'Criador no AssistaPay',avatar:saved.avatar||user.avatar||'',followers:Number(saved.followers||user.followers||0),following:Number(saved.following||user.following||0),likes:Number(saved.likes||user.likes||0)};}
function initials(name){return String(name||'AP').trim().split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase()||'AP';}
function avatarMarkup(p,className='ap-profile-avatar-lg'){return p.avatar?`<div class="${className}"><img src="${esc(p.avatar)}" alt="Foto de perfil"></div>`:`<div class="${className}">${esc(initials(p.name))}</div>`;}
async function publications(){try{return await AP.publicationStore?.listPublished?.()||[];}catch{return [];}}
function gridItem(item){const media=item?.mediaBlob||item?.media||item?.videoUrl||item?.image||'';if(media){const safe=esc(media);return String(item.mediaType||item.type||'').includes('image')?`<div class="ap-profile-media"><img src="${safe}" alt=""></div>`:`<div class="ap-profile-media"><video src="${safe}" muted playsinline preload="metadata"></video><span>▶</span></div>`;}return '<div class="ap-profile-media ap-profile-placeholder">▶</div>';}
async function fillGrid(container,tab){const grid=container.querySelector('.ap-profile-grid');if(!grid)return;const all=await publications();const user=AP.session?.user?.()||{};const own=all.filter(x=>!user.id||String(x.authorId||x.userId||'')===String(user.id));let rows=own;if(tab==='saved'){const ids=AP.profileFavorites?.all?.()||[];rows=all.filter(x=>ids.includes(String(x.id)));}if(tab==='liked'){let liked={};try{liked=JSON.parse(localStorage.getItem('ap:feed-likes')||'{}')}catch{}rows=all.filter(x=>liked[x.id]);}grid.innerHTML=rows.length?rows.map(gridItem).join(''):`<div class="ap-profile-empty"><strong>Nenhum item</strong><span>Aqui aparecerão seus ${tab==='saved'?'salvos':tab==='liked'?'curtidos':'vídeos'}.</span></div>`;}
function resizeAvatar(file){return new Promise((resolve,reject)=>{if(!file||!file.type.startsWith('image/')){reject(new Error('Selecione uma imagem válida.'));return;}if(file.size>10*1024*1024){reject(new Error('A imagem deve ter no máximo 10 MB.'));return;}const reader=new FileReader();reader.onerror=()=>reject(new Error('Não foi possível ler a imagem.'));reader.onload=()=>{const img=new Image();img.onerror=()=>reject(new Error('Não foi possível abrir a imagem.'));img.onload=()=>{const size=420,canvas=document.createElement('canvas');canvas.width=size;canvas.height=size;const ctx=canvas.getContext('2d');const scale=Math.max(size/img.width,size/img.height);const width=img.width*scale,height=img.height*scale;ctx.drawImage(img,(size-width)/2,(size-height)/2,width,height);resolve(canvas.toDataURL('image/jpeg',.82));};img.src=reader.result;};reader.readAsDataURL(file);});}
function editSheet(container,p){document.getElementById('apProfileEdit')?.remove();const sheet=document.createElement('section');sheet.id='apProfileEdit';sheet.className='ap-profile-edit-sheet open';sheet.innerHTML=`<div class="ap-profile-edit-backdrop" data-close></div><form class="ap-profile-edit-card"><header><strong>Editar perfil</strong><button type="button" data-close>×</button></header><div class="ap-profile-photo-editor"><div class="ap-profile-photo-preview">${p.avatar?`<img src="${esc(p.avatar)}" alt="Prévia da foto">`:`<span>${esc(initials(p.name))}</span>`}</div><label class="ap-profile-photo-button">Alterar foto<input type="file" name="avatarFile" accept="image/*" hidden></label><button class="ap-profile-photo-remove" type="button" data-remove-photo ${p.avatar?'':'hidden'}>Remover foto</button></div><label>Nome<input name="name" maxlength="50" value="${esc(p.name)}" required></label><label>Usuário<input name="username" maxlength="30" value="${esc(p.username)}" required></label><label>Bio<textarea name="bio" maxlength="160">${esc(p.bio)}</textarea></label><p class="ap-profile-edit-error" role="alert"></p><button class="ap-profile-save" type="submit">Salvar alterações</button></form>`;document.body.appendChild(sheet);let nextAvatar=p.avatar||'';const preview=sheet.querySelector('.ap-profile-photo-preview'),removeButton=sheet.querySelector('[data-remove-photo]'),error=sheet.querySelector('.ap-profile-edit-error');sheet.addEventListener('click',e=>{if(e.target.closest('[data-close]'))sheet.remove();if(e.target.closest('[data-remove-photo]')){nextAvatar='';preview.innerHTML=`<span>${esc(initials(p.name))}</span>`;removeButton.hidden=true;}});sheet.querySelector('[name="avatarFile"]').addEventListener('change',async e=>{const file=e.target.files?.[0];if(!file)return;error.textContent='';try{nextAvatar=await resizeAvatar(file);preview.innerHTML=`<img src="${nextAvatar}" alt="Prévia da foto">`;removeButton.hidden=false;}catch(err){error.textContent=err.message||'Não foi possível alterar a foto.';e.target.value='';}});sheet.querySelector('form').addEventListener('submit',e=>{e.preventDefault();const data=new FormData(e.currentTarget),current=readProfile();try{writeProfile({...current,name:String(data.get('name')).trim(),username:String(data.get('username')).trim(),bio:String(data.get('bio')).trim(),avatar:nextAvatar});sheet.remove();render(container);}catch(err){error.textContent='A imagem é muito grande para ser salva. Escolha outra foto.';}});}
async function shareProfile(){const url=location.href;try{if(navigator.share)await navigator.share({title:'Meu perfil no AssistaPay',url});else if(navigator.clipboard)await navigator.clipboard.writeText(url);}catch{}}
function render(container){const p=profile();container.innerHTML=`<main class="ap-profile-page"><header class="ap-profile-header"><a href="../feed.html" aria-label="Voltar">‹</a><h1>Perfil</h1><button type="button" data-share-profile aria-label="Compartilhar perfil">↗</button></header><section class="ap-profile-hero">${avatarMarkup(p)}<h2 class="ap-profile-name">${esc(p.name)}</h2><p class="ap-profile-username">${esc(p.username)}</p><p class="ap-profile-bio">${esc(p.bio)}</p></section><section class="ap-profile-stats"><div class="ap-profile-stat"><strong>${p.following}</strong><span>Seguindo</span></div><div class="ap-profile-stat"><strong>${p.followers}</strong><span>Seguidores</span></div><div class="ap-profile-stat"><strong>${p.likes}</strong><span>Curtidas</span></div></section><section class="ap-profile-actions"><button class="primary" type="button" data-edit-profile>Editar perfil</button><a class="secondary" href="app.html?view=showcase">Minha vitrine</a></section><section class="ap-profile-menu"><a href="app.html?view=showcase"><span class="icon">🛍</span><span><strong>Minha Vitrine</strong><small>Produtos e indicações</small></span><em>›</em></a><a href="app.html?view=seller-center"><span class="icon">▦</span><span><strong>Centro do Vendedor</strong><small>Vendas, comissões e produtos</small></span><em>›</em></a><a href="app.html?view=wallet"><span class="icon">◈</span><span><strong>Carteira</strong><small>Saldo e movimentações</small></span><em>›</em></a></section><nav class="ap-profile-tabs"><button class="active" data-tab="videos" type="button">Vídeos</button><button data-tab="saved" type="button">Salvos</button><button data-tab="liked" type="button">Curtidos</button></nav><section class="ap-profile-grid" aria-label="Conteúdo do perfil"></section></main>`;container.onclick=e=>{const tab=e.target.closest('[data-tab]');if(tab){container.querySelectorAll('[data-tab]').forEach(b=>b.classList.toggle('active',b===tab));fillGrid(container,tab.dataset.tab);return;}if(e.target.closest('[data-edit-profile]'))editSheet(container,profile());if(e.target.closest('[data-share-profile]'))shareProfile();};fillGrid(container,'videos');}
AP.profileUI=Object.freeze({render,profile});
})(window);