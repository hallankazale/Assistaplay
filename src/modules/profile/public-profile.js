(function(g){'use strict';const AP=g.AssistaPay=g.AssistaPay||{};const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));function open(id){const user=(AP.searchStore?.users?.()||[]).find(u=>String(u.id)===String(id));if(!user)return false;const host=document.getElementById('appPage');if(!host)return false;host.innerHTML=`<main class="ap-page"><header class="ap-page-header"><a href="app.html?view=search">‹</a><h1>Perfil</h1></header><section style="padding:24px;text-align:center">${user.avatar?`<img src="${esc(user.avatar)}" style="width:96px;height:96px;border-radius:50%;object-fit:cover">`:'<div style="font-size:64px">👤</div>'}<h2>${esc(user.name)}</h2><p>@${esc(user.username||'usuario')}</p><button id="apFollowPublic">Seguir</button></section></main>`;host.querySelector('#apFollowPublic')?.addEventListener('click',e=>{const ids=AP.storage?.get?.('following',[])||[];if(!ids.includes(user.id))ids.push(user.id);AP.storage?.set?.('following',ids);e.currentTarget.textContent='Seguindo';});return true;}AP.publicProfile=Object.freeze({open});})(window);