(function(g){'use strict';const AP=g.AssistaPay=g.AssistaPay||{};const KEY='favorites';const all=()=>AP.storage?.get?.(KEY,[])||[];const has=id=>all().includes(String(id));const toggle=id=>{const key=String(id);const rows=all();const next=rows.includes(key)?rows.filter(x=>x!==key):[...rows,key];AP.storage?.set?.(KEY,next);return next.includes(key);};AP.profileFavorites=Object.freeze({all,has,toggle});})(window);