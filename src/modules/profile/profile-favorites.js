(function(g){'use strict';const AP=g.AssistaPay=g.AssistaPay||{};const KEY='favorites';const read=()=>AP.storage?.read?.(KEY,[])||[];const write=v=>AP.storage?.write?.(KEY,v);const all=()=>read().map(String);const has=id=>all().includes(String(id));const toggle=id=>{const key=String(id);const rows=all();const next=rows.includes(key)?rows.filter(x=>x!==key):[...rows,key];write(next);return next.includes(key);};AP.profileFavorites=Object.freeze({all,has,toggle});})(window);